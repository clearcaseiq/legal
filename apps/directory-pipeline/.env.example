# Attorney Directory Pipeline - separate from ClearCaseIQ
DIRECTORY_PIPELINE_DATABASE_URL=postgresql://user:password@localhost:5432/directory_pipeline

# Rate limit (seconds between requests) - be conservative
# RATE_LIMIT_DELAY_SECONDS=3
